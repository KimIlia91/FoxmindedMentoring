@model IEnumerable<CoverType>
@{
    ViewData["Title"] = "Index";
}

@*@if(TempData["success"] != null)
{
    <h2>@TempData["success"]</h2>
} топорный метод вывода сообщений*@

@*<partial name="_NotificationPartial" /> только для этой страницы*@

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Cover type list</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="CoverType" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i>&nbsp; Create cover type
            </a>
        </div>
    </div>
    <br />
<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>Cover type name</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model)
        {
            <tr>
                <td width="90%">
                    @item.Name
                </td>
                <td>
                    <div class="w-75 btn-group" role="group">
                            <a asp-controller="CoverType" asp-action="Edit" asp-route-id="@item.Id"
                            class="btn btn-primary mx-2">
                                <i class="bi bi-pencil-square"></i>
                            </a>
                            <a asp-controller="CoverType" asp-action="Delete" asp-route-id="@item.Id"
                                class="btn btn-danger mx-0">
                                <i class="bi bi-trash3-fill"></i>
                            </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>
